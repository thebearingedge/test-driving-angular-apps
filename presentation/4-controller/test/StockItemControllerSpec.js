'use strict';

describe('StockItemController', function () {

  var $rootScope,
      $location,
      stockItemController,
      mockItemService,
      stockItemService,
      myGuitar;

  beforeEach(function () {

    // initialize mock service object and data

    // provide new definition of stockItemService
    module('exampleApp');

    // build mock data service
    inject(function () {
    });

    // inject and reference services
    inject(function () {
    });

    // instantiate and initialize controller with $controller
    inject(function () {
    });


  });


  describe('#item', function () {

    it('should immediately publish stock item data to the view');

  });

  describe('#saveItem', function () {

    it('should save a stock item and redirect to root');

  });

});